# horse2zebra を題材にしたマスクSAE実験メモ

## 目的

施設間差(ここでは「馬」対「シマウマ」に見立てる)を、SAEの特定latentに集約できるかを検証する。CIFAR-100には馬もシマウマも無く解像度も足りないため、horse2zebraデータセットで代替する。

## 対応関係

| 本番(病理画像を想定) | この実験 |
|---|---|
| 施設A / 施設B | 馬の画像 / シマウマの画像 |
| 施設間の差 | 縞模様・毛色 |
| 施設差以外の内容 | 構図、背景、姿勢、頭数 |
| A'(CycleGANでBライクにしたA) | CycleGANで縞を付けた馬 |
| 基盤モデル | ImageNetで学習したViT-B/16 or CLIP画像エンコーダ(OpenCLIP-ViT-H-14) |
| 下流タスク | 分類ヘッドが「シマウマ」と判定するか(ViT-B/16使用時のみ) |

## データ・モデル

- horse2zebra: 馬(train 1067, test 120)、シマウマ(train 1334, test 140)、256x256
- 学習済みCycleGAN(馬→シマウマ)が公開されており、A'生成にはこれを使う(GAN自体の学習は不要)
- 埋め込み抽出はViT-B/16(ImageNet分類済み)で開始。定性評価をIP-Adapter経由のStable Diffusionで行う場合は、CLIP画像エンコーダ(OpenCLIP-ViT-H-14, SD1.5用)に差し替える必要がある。**SAE学習に使うエンコーダとIP-Adapterが前提とするエンコーダは一致させること。**
- 学習データは左右反転で2倍(AとA'は同じ反転を適用しペア関係を保持)

## Step 0: 学習前の確認(SAE学習前に必ず実施)

1. mean(Δ) = mean(emb_a' − emb_a) と 実データの重心差 δ = mean(emb_b) − mean(emb_a) のコサイン類似度・ノルム比を確認。1に近いほどGANの変化方向が正しい。
2. emb_a' と 本物のemb_b の識別AUROC(線形・RBF)。0.5に近いほどA'は本物らしい。高いとGAN由来のアーティファクトを基盤モデルが検出している可能性。
3. シマウマ判定率(ViT使用時): A, A', Bで比較。
4. Δ = emb_a' − emb_a のうち定数シフト(mean(Δ))で説明できる割合R。1に近いほど施設差はほぼ平行移動＝重心補正で足りる。低いほどサンプル依存成分がありSAEの価値が出る。
5. 「馬らしさ」の確率とΔのノルムの順位相関。CycleGANは馬が大きく写っているほど変化が大きいはずなので、施設差がサンプル依存になる具体的な根拠になる。

## SAEアーキテクチャ(マスク案)

共有エンコーダ1本を emb_a, emb_a' の両方に通す(モデルは同一、重み共有)。latentを施設latent z_S と共有latent z_shared に分割。

```
u = (x - b_pre) @ W_enc + b_enc
u_S, u_sh = u[:, :nS], u[:, nS:]
z_S  = ReLU(u_S)  * (A'のときは強制的に0)   # マスク
z_sh = ReLU(u_sh)                            # or TopK
x_hat = concat(z_S, z_sh) @ W_dec + b_dec    # デコーダは1本(共有)
```

損失:

```
loss = mse(x_hat_a, x_a) + mse(x_hat_a2, x_a2)
     + lam_l1 * (z_S_a.sum() + z_sh_a.sum() + z_sh_a2.sum())      # 疎性
     + lam_pair * ((z_sh_a - z_sh_a2) ** 2).sum(-1).mean()         # 共有latentの一致
```

- 実験レベルでは新規性は求めず、シンプルな構成(ReLU+L1、TopKも可)で良い。AuxK損失は最初は不要(dead latentが実際に問題になってから検討)。
- 最初はハードマスク(A'で強制的に0)で開始。本物のBでも施設latentが正しく発火するかを後で確認し、問題があればA'側での活性にソフトな罰則をかける方式に切り替える。
- 辞書サイズはペア数(2000弱)に対して大きくしすぎない(拡大率2〜8倍程度が目安)。

## 定量評価

| 指標 | 見るもの |
|---|---|
| Δ説明率 | 1 − ‖Δ − Σ寄与‖² / ‖Δ‖²。施設latentがΔをどれだけ再現できたか |
| 施設識別AUROC(施設latentのみ) | z_Sだけで本物のA/Bを識別できるか(高いほど施設検出器として機能) |
| 施設識別AUROC(共有latentのみ) | 漏れの確認。0.5に近いほど内容側に施設差が漏れていない |
| ペア検索 | 補正後の馬に最も近いA'が自分のペアかどうか(内容保存の確認) |
| latent数の振り方(|S|=1,4,16) | 施設latentの数を増やして回収率が頭打ちになるか確認。Step0のRと整合するはず |

比較対象として、重心補正のみ・Ridge回帰・A'そのもの(教師)も並べる。

## 定性評価(定量が良好な場合のみ実施)

IP-Adapter(`h94/IP-Adapter`, apache-2.0) + Stable Diffusion 1.5(`stable-diffusion-v1-5/stable-diffusion-v1-5`, CreativeML OpenRAIL-M)を利用。

- IP-Adapter本体・CLIP画像エンコーダ・SD1.5は全て公開の学習済み重みをダウンロードするだけで良く、追加学習は不要。**訓練が必要なのはSAEのみ。**
- テスト画像の馬 → CLIP画像エンコーダ → emb_a取得 → SAEで施設latentを除去 → emb_shared → ノルムを元のemb_aと揃える → IP-Adapterの射影ネットワークに直接入力 → SD1.5で生成
- emb_a(施設latent込み)、emb_shared(施設latent除去)、emb_a'(教師)の3通りで生成し比較。emb_sharedが「縞のない、内容は保たれた馬」に近ければ仮説の視覚的裏付けになる。
- 生成が崩れる場合、emb_sharedの分布が射影ネットワークの学習時分布から外れている可能性。ノルム調整で直らなければ射影ネットワークのみ軽く追加学習することも検討(最終手段)。
- SD1.5を選んだ理由: 計算コストが軽い、horse2zebraの元解像度(256x256)と釣り合いが取れる。細部の縞模様の判別が難しい場合はSDXLへの切り替えを検討(その場合CLIPエンコーダをOpenCLIP-ViT-bigG-14に差し替え、SAEも再学習が必要)。

## この例の限界

- シマウマはImageNet/CLIPの学習データで強く意識されている概念であり、本番の施設差(撮影機器由来の色調など)よりも基盤モデルが線形に読み取りやすい可能性が高い。ここでうまくいっても本番でうまくいく保証にはならない。
- 施設差と無関係な「下流ラベル」が無いため、施設latentが内容情報を持たないこと(交絡がないこと)の検証はペア検索による間接確認にとどまる。

## 関連研究(位置づけの参考)

- 疎な辞書学習でのドメイン分離: Domain-Shared Group-Sparse Dictionary Learning(Yang et al.)、A Dictionary Approach to Domain-Invariant Learning。ただし構造は逆(ドメイン固有がアトム側、共有が係数側)。
- 密なオートエンコーダでの共有/固有分離: Domain Separation Networks(直交性損失)、単一細胞バッチ補正(scMEDAL, Kanade, ABC: 敵対的損失)。
- 疎性を保った事後発見: Crosscoder(Lindsey et al., 2024)。マスクによる強制はなく、学習後にデコーダノルム比で施設固有latentを判定する。
- 今回のマスク案(疎性+学習時の明示的なマスク+ペア一致損失)の完全な先行例は未確認。実験目的なので新規性は狙わない。
