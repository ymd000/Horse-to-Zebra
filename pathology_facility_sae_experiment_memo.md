# 病理画像(2施設)を題材にしたマスクSAE実験メモ

horse2zebra実験(`horse_zebra_experiment_memo.md`)と同じ枠組みを、実データである2施設の病理画像に適用する。基盤モデルやエンコーダの具体的な選定はここでは扱わない。

## 前提

- 画像は施設A、施設Bの2種類。同じ対象(組織)を撮影しているが、染色・スキャナ等に起因する施設間差がある。
- 病理基盤モデルに通した埋め込みをemb_a, emb_bとする。
- 下流タスクにおいて、emb_aとemb_bの施設間差は重心補正でかなり攻略できることが既に確認されている → 施設差の大部分は定数シフトδ = mean(emb_b) − mean(emb_a)で説明できる可能性が高い。SAEに期待する価値は主に以下:
  1. δを解釈可能な複数成分に分解できるか
  2. サンプルごとに異なる施設差(重心補正で拾えない残差)を捉えられるか
  3. 施設差の成分だけを狙って補正できるか(下流に不要な情報を壊さない補正)

## A' の作り方(horse2zebraとの対応)

horse2zebraではCycleGANで馬→シマウマ化したA'を教師として使った。病理画像でも同様に、画像レベルのドメイン変換(CycleGAN等)で施設Aの画像を施設Bライクにした A' を用意し、emb_a'を得る。

- ペア(同一対象を両施設で撮影したもの)が実際に存在する場合は、学習には使わずSAE・変換モデルの最終検証に温存するのが望ましい(合成A'のバイアスから独立した評価ができる)。

## Step 0: 学習前の確認(病理データでも必須)

horse2zebraと同じ4点セットを、病理の埋め込みで最初に確認する。

1. mean(Δ) と 実データの重心差δ の一致度(コサイン類似度・ノルム比)。GAN変換が実際の施設差と同じ方向・大きさに動いているか。
2. emb_a' と 本物のemb_b の識別しやすさ(AUROC)。高い場合、基盤モデルがGAN由来のアーティファクトを検出しており、A'を教師とすることの信頼性に疑問符がつく。
3. 下流タスク性能(Bで学習したprobeをA'に適用した場合とAに適用した場合を比較)。内容(病理学的に意味のある情報)がGAN変換で保存されているかの確認。異なる構造が生成される既知のリスクがあるため重要。
4. Δのうち定数シフトで説明できる割合R。既に「重心補正で攻略できる」という結果があるため、Rは高く出る可能性がある。その場合、SAElatentへの期待は「解釈」寄りになり、「疎な成分の発見」という主張は弱まる可能性がある点をあらかじめ想定しておく。

## SAEアーキテクチャ

horse2zebraと同一の構成をそのまま流用する(マスク+ペア一致損失+疎性)。

```
u = (x - b_pre) @ W_enc + b_enc
u_S, u_sh = u[:, :nS], u[:, nS:]
z_S  = ReLU(u_S)  * (A'のときは強制的に0)
z_sh = ReLU(u_sh)
x_hat = concat(z_S, z_sh) @ W_dec + b_dec

loss = mse(x_hat_a, x_a) + mse(x_hat_a2, x_a2)
     + lam_l1 * (z_S_a.sum() + z_sh_a.sum() + z_sh_a2.sum())
     + lam_pair * ((z_sh_a - z_sh_a2) ** 2).sum(-1).mean()
```

horse2zebraでの実験結果(特にどのlam_pair・辞書サイズ・マスクの厳しさで回収率と漏れのバランスが良かったか)をハイパーパラメータの出発点として使う。

## 定量評価(horse2zebraと共通の指標)

| 指標 | 見るもの |
|---|---|
| Δ説明率 | 施設latentがΔ(施設差)をどれだけ再現できたか |
| 施設識別AUROC(施設latentのみ) | z_Sだけで本物の施設A/Bを識別できるか |
| 施設識別AUROC(共有latentのみ) | 内容側への施設差の漏れがないか |
| 下流タスク性能(共有latentのみ、または補正後embで再学習) | 施設差を除いても病理学的な判定精度が保たれるか。**病理では最重要の確認項目** |
| クラス別の層別確認 | 施設とラベル(診断など)の分布が施設間で異なる場合、施設latentがラベル情報を拾っていないか層別して確認(交絡の排除) |
| 第三の施設での汎化 | 施設Cのデータがあれば、同じ施設latentが効くかを確認。最も説得力のある検証 |

- horse2zebraには無かった「下流ラベル」がここでは存在するため、施設latentが内容(診断ラベル)の情報を持たないことを直接検証できる。これは前段の実験でできなかった検証であり、病理データでの実験の主要な追加価値。
- ペアデータ(同一対象を両施設で撮影)が入手できれば、ペア間距離の縮小を最も直接的な指標として追加する。

## 定性評価について

- 病理画像は自然画像(馬・シマウマ)と異なり、汎用の拡散モデル(Stable Diffusion系)やIP-Adapterがそのまま使える保証はない。病理画像向けの生成モデルが利用可能か、または画像レベルでの定性評価自体を見送り、latentが強く発火するパッチ画像の一覧(前々回の`interpret`関数のような手法)による確認に留めるかを別途検討する。
- 実施する場合も、horse2zebraと同じ考え方(emb_a、emb_shared、emb_a'の3通りを比較)を踏襲する。

## horse2zebraとの違い・注意点

- horse2zebraでは施設差(縞模様)が基盤モデルにとって強く・線形に読み取りやすい概念だった可能性が高い。病理の施設差(染色・スキャナ由来)はより微妙で、基盤モデルが学習時に意識していない差である可能性があり、horse2zebraでの好結果がそのまま病理データに移行するとは限らない。
- horse2zebraでは下流ラベルがなく交絡の検証ができなかったが、病理データでは診断ラベル等が存在するため、H3相当(施設latentが下流ラベル情報を持たないこと)の検証を必ず行う。
- 重心補正が既に強い効果を示している前提があるため、Step 0でRが高く出た場合の解釈(SAEの価値は主に解釈性であり、疎な成分の存在を主張するのは慎重に)をあらかじめ想定しておく。

## 進め方

1. horse2zebraでの実験で、マスク+ペア一致損失の組み合わせが機能する(回収率が高く、漏れが小さい)ことを先に確認する。
2. 機能したハイパーパラメータ・設計を出発点に、病理の埋め込みでStep 0から同じ手順を繰り返す。
3. 病理データ特有の追加検証(下流ラベルとの交絡確認、第三施設での汎化)を重点的に行う。
